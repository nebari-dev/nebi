services:
  nebi-team:
    build: .
    container_name: nebi-team-dev
    ports:
      - "8470:8460"
    environment:
      NEBI_DATABASE_DSN: /app/data/nebi-team.db
      NEBI_AUTH_JWT_SECRET: dev-secret-that-is-at-least-32-chars-long
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: admin123
    volumes:
      - nebi_team_data:/app/data

volumes:
  nebi_team_data:
