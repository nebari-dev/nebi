/**
 * localStorage key nebi's theme preference persists under. Passed to the
 * registry-managed `ThemeProvider` (and mirrored by the pre-paint script in
 * `index.html`) so existing users keep their saved preference.
 */
export const THEME_MODE_STORAGE_KEY = 'nebi:themeMode';
