/**
 * `localStorage` key the theme preference persists under. Kept from the
 * original hand-rolled hook so existing users keep their saved preference.
 */
export const THEME_STORAGE_KEY = 'nebi:themeMode';
